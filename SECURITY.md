# سیاست امنیتی

لطفاً آسیب‌پذیری‌ها را در issue عمومی گزارش نکنید و token، URL دانلود حاوی token یا credentials را در هیچ گزارش عمومی قرار ندهید.

اگر گزینهٔ **Report a vulnerability** در تب Security مخزن GitHub فعال است، GitHub Private Security Advisory مسیر ترجیحی گزارش است. در غیر این صورت، گزارش را با موضوع روشن به `sajaddp71@gmail.com` ارسال کنید.

گزارش مفید شامل نسخه یا commit پکیج، نسخهٔ Laravel و PHP، شرح اثر، reproduction حداقلی، و در صورت امکان راهکار پیشنهادی است. از ارسال token واقعی خودداری کنید.

تا نخستین release پایدار، فقط شاخهٔ توسعهٔ فعلی پشتیبانی می‌شود. هیچ زمان پاسخ یا رفع مشخصی تضمین نمی‌شود.
