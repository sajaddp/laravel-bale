# مشارکت در Laravel Bale

از مشارکت شما استقبال می‌شود. این پکیج Laravel 13 و PHP 8.3+ را هدف می‌گیرد و عمداً کم‌حجم باقی می‌ماند.

پیش از تغییر رفتار Bale، HTML مستندات رسمی Bale را بررسی کنید. از شباهت نام‌ها با Telegram، API یا قرارداد آن استنباط نکنید. انتزاع آینده‌نگر، چارچوب جدید یا متد کمکی تازه فقط وقتی پذیرفتنی است که روندی تکراری و خطاپذیر را با ابزارهای موجود ساده کند.

برای تغییر کد:

1. تستِ رفتار قابل مشاهده را با شبیه‌سازی HTTP در Laravel اضافه یا به‌روزرسانی کنید؛ به شبکهٔ واقعی وصل نشوید.
2. `composer test` را اجرا کنید.
3. اگر API عمومی تغییر کرده است، README، PHPDoc Facade، راهنمای Boost، مهارت و [مرجع API](docs/api-reference.md) را همگام کنید.
4. اگر متد رسمی تازه‌ای اضافه شده است، [پوشش API](docs/api-coverage.md) را بر اساس مستندات Bale اصلاح کنید.

## مستندات سایت

Python فقط برای ساخت یا ویرایش سایت مستندات لازم است، نه برای مصرف پکیج. در یک محیط مجازی جداگانه وابستگی‌های مستندات را نصب کنید و پیش از PR ساخت سخت‌گیرانه را اجرا کنید:

~~~shell
python3 -m venv .venv-docs
source .venv-docs/bin/activate
pip install -r requirements-docs.txt
mkdocs serve
mkdocs build --strict
~~~

مسائل امنیتی را در issue عمومی مطرح نکنید؛ [SECURITY.md](SECURITY.md) را ببینید. برای آشنایی با پکیج به [مستندات](docs/index.md) و برای فهرست بررسی PR به [template](.github/PULL_REQUEST_TEMPLATE.md) مراجعه کنید.
