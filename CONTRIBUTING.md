# مشارکت در لاراول بله

از مشارکت شما استقبال می‌شود. این پکیج برای لاراول ۱۳ و پی‌اچ‌پی ۸٫۳ یا جدیدتر است و عمداً کم‌حجم باقی می‌ماند.

پیش از تغییر رفتار بله، متن اچ‌تی‌ام‌ال مستندات رسمی بله را بررسی کنید. از شباهت نام‌ها با تلگرام، رابط برنامه‌نویسی یا قرارداد آن نتیجه‌گیری نکنید. انتزاع آینده‌نگر، چارچوب تازه یا متد کمکی جدید فقط وقتی پذیرفتنی است که روندی تکراری و خطاپذیر را با ابزارهای موجود ساده کند.

برای تغییر کد:

1. آزمونِ رفتار قابل مشاهده را با شبیه‌سازی درخواست اچ‌تی‌تی‌پی در لاراول اضافه یا به‌روزرسانی کنید؛ به شبکهٔ واقعی وصل نشوید.
2. `composer test` را اجرا کنید.
3. اگر رابط برنامه‌نویسی عمومی تغییر کرده است، راهنمای اصلی، توضیحات کلاس دسترسی، راهنمای بوست، مهارت و [مرجع رابط برنامه‌نویسی](docs/api-reference.md) را همگام کنید.
4. اگر متد رسمی تازه‌ای اضافه شده است، [پوشش رابط برنامه‌نویسی](docs/api-coverage.md) را بر اساس مستندات بله اصلاح کنید.

## مستندات سایت

پایتون فقط برای ساخت یا ویرایش سایت مستندات لازم است، نه برای استفاده از پکیج. در یک محیط مجازی جداگانه وابستگی‌های مستندات را نصب کنید و پیش از درخواست ادغام، ساخت سخت‌گیرانه را اجرا کنید:

~~~shell
python3 -m venv .venv-docs
source .venv-docs/bin/activate
pip install -r requirements-docs.txt
mkdocs serve
mkdocs build --strict
~~~

مسائل امنیتی را در مسئلهٔ عمومی مطرح نکنید؛ [SECURITY.md](SECURITY.md) را ببینید. برای آشنایی با پکیج به [مستندات](docs/index.md) و برای فهرست بررسی درخواست ادغام به [الگو](.github/PULL_REQUEST_TEMPLATE.md) مراجعه کنید.
